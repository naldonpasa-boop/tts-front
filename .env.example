# Which backend client to wire: "dummy" (default, static in-memory snapshot) or "real".
VITE_SCENE_API=dummy

# Base URL of the real backend. Used only when VITE_SCENE_API=real.
# The client calls GET {VITE_API_BASE_URL}/api/scene/snapshot
VITE_API_BASE_URL=http://localhost:8080
